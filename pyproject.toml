[project]
name = "f1-live"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastf1>=3.8.1",
]

[project.optional-dependencies]
dev = [
    "black>=26.3.1",
]
